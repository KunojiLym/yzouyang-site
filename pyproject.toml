[project]
name = "yzouyang-site"
version = "0.1.0"
description = "Build scripts for the yzouyang.com static site (not a distributable package)."
requires-python = ">=3.10"
dependencies = [
  "pyyaml>=6.0",
]

[tool.uv]
package = false
